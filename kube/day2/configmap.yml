apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
data:
  DEFAULT.conf: |
    server {
        listen       80;
        location / {
           return 200 'Hello from Nginx in APP_MODE=${APP_MODE}';
        }
    }
